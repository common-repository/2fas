{% extends "dashboard/admin/admin-base.html.twig" %}

{% block page %}
    <div class="twofas-account-box-wrapper">
        <div class="twofas-account-box">
            <div class="twofas-account-box-header">
                <h3>{{ esc_html__('Reset Password', '2fas') }}</h3>
            </div>
            <div class="twofas-account-box-content">
                <form action="{{ create_url(constant('TwoFAS\\TwoFAS\\Http\\Action_Index::SUBMENU_DASHBOARD'), constant('TwoFAS\\TwoFAS\\Http\\Action_Index::ACTION_RESET_PASSWORD')) }}" method="post">
                    {{ create_form_nonce(constant('TwoFAS\\TwoFAS\\Http\\Action_Index::ACTION_RESET_PASSWORD')) | raw }}
                    <label for="twofas-email">{{ esc_html__('Your 2FAS e-mail', '2fas') }}:</label>
                    <input type="email" class="js-autofocus" id="twofas-email" name="twofas[email]" value="{{ email }}" required="required" autofocus="autofocus" />
                    <div class="twofas-submit-container">
                        <input type="submit" class="twofas-btn twofas-account-box-btn" value="{{ esc_attr__('Reset Password', '2fas') }}" />
                    </div>
                </form>
                <hr />
                <a href="{{ create_url(constant('TwoFAS\\TwoFAS\\Http\\Action_Index::SUBMENU_DASHBOARD'),
                    constant('TwoFAS\\TwoFAS\\Http\\Action_Index::ACTION_LOGIN')) }}">{{ esc_html__('I have a 2FAS account', '2fas') }}</a>

                <a href="{{ create_url(constant('TwoFAS\\TwoFAS\\Http\\Action_Index::SUBMENU_DASHBOARD'),
                    constant('TwoFAS\\TwoFAS\\Http\\Action_Index::ACTION_CREATE_ACCOUNT')) }}">{{ esc_html__('Create an account', '2fas') }}</a>
            </div>
        </div>
    </div>
{% endblock %}
